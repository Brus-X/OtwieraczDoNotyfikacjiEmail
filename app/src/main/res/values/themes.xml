<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Podstawowy motyw aplikacji -->
    <style name="Theme.OtwieraczDoNotyfikacjiEmail" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Możesz tu dodać kolory główne, jeśli chcesz -->
    </style>

    <!-- Motyw dla Splash Screena -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="postSplashScreenTheme">@style/Theme.OtwieraczDoNotyfikacjiEmail</item>
    </style>
</resources>